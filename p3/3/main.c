#include <stdio.h>
#include <stdlib.h>

int main()
{
    int i=0;
    float n,sum,miangin;
    while(n!=0)
     {
        printf("adad ra vared konid = ");
        scanf("%f",&n);
        sum=sum+n;
        i++;
     }
    miangin=sum/(i-1);
    printf("\n miangine adade vared shode = %f",miangin);
    return 0;
}
